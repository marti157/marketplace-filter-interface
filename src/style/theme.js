import { createTheme } from '@mui/material/styles';

export default createTheme({
  palette: {
    primary: {
      main: '#52528C',
    },
    secondary: {
      main: '#9DD1F1',
    },
    error: {
      main: '#DC493A',
    },
  },
  shadows: [0, 0, 0, 0, 0],
});
